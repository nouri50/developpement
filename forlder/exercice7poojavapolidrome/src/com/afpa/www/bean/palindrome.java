package com.afpa.www.bean
public class palindrome {
    public static String inverse(String S){
    StringBuffer envers=new StringBuffer();
    for (int i s.length()-1;i>=0 i--){
        envers.append(s.charAT(i));
        }
    return String(envers);
    }

}
